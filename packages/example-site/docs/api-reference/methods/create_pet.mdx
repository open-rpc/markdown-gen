---
title: "create_pet"
hide_table_of_contents: true
description: "Creates a new pet in the store.  Duplicates are allowed"
sidebar_label: "create_pet"
rpc_method: "create_pet"
tags:
  - json-rpc
  - openrpc
  - method
---

import {TwoColumnLayout, RequestExample, ResponseExample, TryNow} from '@open-rpc/docusaurus-plugin/components';

<TwoColumnLayout 
  sidebar={
    <>
      <RequestExample 
        title="Named Parameters" 
        code={"{\n  \"jsonrpc\": \"2.0\",\n  \"method\": \"create_pet\",\n  \"params\": {\n    \"newPet\": {\n      \"name\": \"string\",\n      \"tag\": \"string\"\n    }\n  },\n  \"id\": 1\n}"} 
      />
      
      <ResponseExample 
        title="Successful Response" 
        code={"{\n  \"jsonrpc\": \"2.0\",\n  \"result\": \"example\",\n  \"id\": 1\n}"} 
      />
      
      <RequestExample 
        title="Positional Parameters" 
        code={"{\n  \"jsonrpc\": \"2.0\",\n  \"method\": \"create_pet\",\n  \"params\": [\n    {\n      \"name\": \"string\",\n      \"tag\": \"string\"\n    }\n  ],\n  \"id\": 2\n}"} 
      />
      
      <ResponseExample 
        title="Successful Response" 
        code={"{\n  \"jsonrpc\": \"2.0\",\n  \"result\": \"example\",\n  \"id\": 2\n}"} 
      />
    </>
  }
>

<TryNow method={"create_pet"} openrpcDocument={{"name":"create_pet","description":"Creates a new pet in the store.  Duplicates are allowed","params":[{"name":"newPet","description":"Pet to add to the store.","schema":{"type":"object","required":["name"],"properties":{"name":{"type":"string"},"tag":{"type":"string"}}}}],"result":{"name":"pet","description":"the newly created pet","schema":{"allOf":[{"type":"object","required":["name"],"properties":{"name":{"type":"string"},"tag":{"type":"string"}}},{"required":["id"],"properties":{"id":{"type":"integer"}}}]}}}} onClick={()=>(<div>Hello</div>)} />
# create_pet

Creates a new pet in the store.  Duplicates are allowed


---

## Method

- **JSON-RPC method name:** `create_pet`
- **JSON-RPC version:** `2.0`

## Parameters

| Name | Type | Required | Description |
| ---- | ---- | -------- | ----------- |
| newPet | `object` | no | Pet to add to the store. |

### Parameter schema

```json
{
  "newPet": {
    "description": "Pet to add to the store.",
    "required": false,
    "schema": {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "tag": {
          "type": "string"
        }
      }
    }
  }
}
```

## Result

- **Name:** `pet`
- **Description:** the newly created pet
- **Type:** `allOf`

### Result schema

```json
{
  "allOf": [
    {
      "type": "object",
      "required": [
        "name"
      ],
      "properties": {
        "name": {
          "type": "string"
        },
        "tag": {
          "type": "string"
        }
      }
    },
    {
      "required": [
        "id"
      ],
      "properties": {
        "id": {
          "type": "integer"
        }
      }
    }
  ]
}
```


### Positional parameters

Parameter order:

1. `newPet`

---

## Errors

This method may return JSON-RPC standard errors (e.g. `-32600`, `-32601`, `-32602`, `-32603`) as well as application-specific errors.

> TODO: Document method-specific error codes and conditions.

</TwoColumnLayout>
